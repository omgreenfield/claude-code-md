# Feature: inline responses

In situations where the user or agent asks questions or lists decisions to be made, it's often easier to put responses nearby—e.g. as a sub-bullet or inline

Allow for communication to be done in this way.

Look at the `/code-comment-threads` skill for examples on how this could work.

This may require that the CLI keeps track of user and agent blocks and checks for changes, then notify accordingly.
